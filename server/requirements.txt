fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9

pillow==10.4.0
numpy==2.0.2

# biblioteca para escrever formatos de bordado (PES/DST/JEF/EXP/HUS/VP3)
pyembroidery==1.5.1

# parser vetorial para ler propriedades reais do SVG (bbox, fill, stroke, transform)
svgelements==1.9.6

# rasterização de SVG sem depender do Inkscape (fallback principal para .svg)
cairosvg==2.7.1